# Convex Backend

Andromo AI builder provisions this Convex deployment for generated mobile projects.

- Add schema tables in `convex/schema.ts`.
- Add queries and mutations in `convex/*.ts`.
- Do not edit `convex/_generated/*`.
- Run `bun run backend:deploy` after backend changes when the platform deployment needs updating.
